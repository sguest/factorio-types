{
  "name": "factorio-types",
  "version": "0.0.7",
  "description": "Typescript declarations for the factorio mod API",
  "main": "dist/index.d.ts",
  "repository": "https://github.com/sguest/factorio-types.git",
  "author": "Spencer Guest",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -c tslint.json 'dist/**/*.d.ts'",
    "validate": "tsc -p tsconfig.json && yarn run lint",
    "copy-source": "cpy src/*.d.ts dist",
    "build": "ts-node json_parser/parser.ts && yarn run copy-source",
    "rebuild": "ts-node json_parser/parser.ts -clean && yarn run copy-source"
  },
  "devDependencies": {
    "@types/node": "^15.12.2",
    "cpy-cli": "^3.1.1",
    "node-html-parser": "^1.1.16",
    "ts-node": "^10.0.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  },
  "files": [
    "dist/**/*.d.ts"
  ]
}
